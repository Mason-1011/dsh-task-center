{
  "name": "dsh-task-center",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "tsc -p packages/task && tsc -p packages/task-local && tsc -p packages/tool-task && tsc -p packages/command-task && tsc -p packages/task-wake && tsc -p packages/task-quota && tsc -p packages/task-reaper && tsc -p packages/shell",
    "test": "vitest run",
    "start": "node --experimental-transform-types packages/shell/src/bin.ts"
  },
  "devDependencies": {
    "@deepseek-ai/cordis": "^4.0.1",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0-beta",
    "vitest": "^3.0.0"
  }
}
